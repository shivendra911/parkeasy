# ===========================================================
# ParkEasy Backend — Test Profile
# ===========================================================

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/parkeasy_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

jwt:
  secret: test-secret-for-unit-tests-only-must-be-at-least-64-characters-long!!
  expiration-ms: 3600000

logging:
  level:
    com.parkeasy: DEBUG
