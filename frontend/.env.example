# ===========================================================
# ParkEasy Frontend — Environment Variables (EXAMPLE)
# ===========================================================
# Copy this file to .env.local and adjust values.
# .env.local is gitignored — never commit real secrets.
# ===========================================================

VITE_API_BASE_URL=http://localhost:8080/api/v1
